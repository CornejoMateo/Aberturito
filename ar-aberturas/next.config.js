// Esto lo cree solamente porque me lo tiro la consola cuando queria acceder desde mi celular
// Al parecer hay un nuevo parametro para configurar el host en el que corre la app
// No cambia en nada la configuracion actual, solo es para que no me tire el warning
// next.config.js
module.exports = {
  allowedDevOrigins: ['192.168.100.121'],
}